{
  "name": "stillframe-capture",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A free, local-only Chrome screenshot extension.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node scripts/check.mjs",
    "package": "python3 scripts/package.py",
    "dev": "node scripts/serve.mjs"
  },
  "engines": { "node": ">=20" },
  "license": "MIT"
}
